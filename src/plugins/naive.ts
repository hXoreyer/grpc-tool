import { create, NButton, NConfigProvider, NDivider, NDropdown, NEmpty, NIcon, NInput, NInputGroup, NMention, NModal, NNotificationProvider, NPopover, NSpin, NSwitch, NTab, NTabPane, NTabs, NTree } from "naive-ui";

const naive = create({
    components: [
        NButton,
        NIcon,
        NDivider,
        NTree,
        NInput,
        NEmpty,
        NTabs,
        NTabPane,
        NPopover,
        NModal,
        NNotificationProvider,
        NDropdown,
        NInputGroup,
        NSpin
    ]
})

export default naive